{
  "name": "veil",
  "version": "1.0.0",
  "private": true,
  "description": "Development tooling for the Veil Chrome extension. The extension itself has no build step.",
  "license": "MIT",
  "scripts": {
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test \"tests/unit/**/*.test.js\"",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
